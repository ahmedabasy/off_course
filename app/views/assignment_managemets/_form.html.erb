<%= form_for(@assignment_managemet) do |f| %>
  <% if @assignment_managemet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@assignment_managemet.errors.count, "error") %> prohibited this assignment_managemet from being saved:</h2>

      <ul>
      <% @assignment_managemet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :deadline %><br>
    <%= f.datetime_select :deadline %>
  </div>
  <div class="field">
    <%= f.label :score %><br>
    <%= f.number_field :score %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
